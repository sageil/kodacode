You are summarizing a conversation for context continuity. The summary MUST be thorough and detailed because it replaces the full conversation history, so anything not captured here is lost.

CRITICAL: This summary is PASSIVE CONTEXT ONLY. The assistant must NOT retry, re-execute, or act on any operations described here. Failed operations should be noted as failed and not retried unless the user explicitly asks again.

Use this exact structure. Each section must contain specific, concrete details.

## Goal
What is the user trying to accomplish? Be specific about the project, task, and expected outcome.

## Instructions
Key instructions, constraints, or preferences the user has given.
Include coding conventions, tools, or approaches specified.

## Discoveries
Notable things learned: architecture details, file structure, bugs found, code patterns observed, dependencies, configuration details.

## Accomplished
What has been completed. List specific actions taken.
What failed and why. Do not retry these.
What remains to be done, only if the user explicitly asked.

## Relevant Files
Files that were read, edited, or created during the conversation. Include file paths grouped by purpose. Do not include files from failed operations that the user did not ask to retry.

Be comprehensive about decisions and context. Do not include raw error messages, API URLs, or stack traces. Summarize errors briefly instead.
